# CardioAI

**CardioAI** — это демонстрационное веб-приложение, использующее искусственный интеллект для первичного анализа сердечного ритма на основе сейсмокардиографии (SCG). Приложение превращает смартфон в диагностический инструмент, способный фиксировать микровибрации грудной клетки, вызванные работой сердца.

---

## 📂 Структура проекта

Проект разделен на две основные части:

*   **`cardioai_backend/`**: Бэкенд на базе **FastAPI**. Отвечает за оркестрацию данных, взаимодействие с Math API (для анализа сигналов) и Dr7 API (медицинская LLM модель).
*   **`cardioai_frontend/`**: Фронтенд на базе **Next.js**. Обеспечивает интерфейс чата, захват данных с акселерометра смартфона в реальном времени и визуализацию процесса измерения.

---

## ⚙️ Настройка окружения

Для работы приложения необходимо настроить переменные окружения. В каждой папке (`cardioai_backend` и `cardioai_frontend`) есть файл `.env.example`, который можно использовать как шаблон.

### 🐍 Backend (`cardioai_backend/.env`)
| Переменная | Описание |
| :--- | :--- |
| `HEARTSCAN_API_KEY` | Ключ доступа к HeartScan Math API для анализа SCG сигналов. |
| `DR7_API_KEY` | API-ключ Dr7.ai для работы с медицинской языковой моделью. |
| `PORT` | Порт, на котором будет запущен сервер (по умолчанию 8000). |

### ⚛️ Frontend (`cardioai_frontend/.env`)
| Переменная | Описание |
| :--- | :--- |
| `CARDIOAI_BACKEND_URL` | URL вашего развернутого бэкенда (например, на Render). |

---

## 🚀 Локальный запуск

### 1. Запуск Бэкенда
```bash
cd cardioai_backend
python -m venv venv
source venv/bin/activate  # Для Windows: venv\Scripts\activate
pip install -r requirements.txt
python main.py
```

### 2. Запуск Фронтенда
```bash
cd cardioai_frontend
npm install
npm run dev
```

---

## 🌍 Развертывание (Production)

### Бэкенд (Render)
1. Создайте новый **Web Service** на Render.com.
2. Укажите Root Directory: `cardioai_backend`.
3. Build Command: `pip install -r requirements.txt`.
4. Start Command: `python main.py`.
5. Добавьте API-ключи в разделе **Environment Variables**.

### Фронтенд (Vercel)
1. Создайте новый проект на Vercel.com.
2. Укажите Root Directory: `cardioai_frontend`.
3. Framework Preset: `Next.js`.
4. Добавьте `CARDIOAI_BACKEND_URL` в переменные окружения.
5. Нажмите **Deploy**.

---

## 📚 Полезные ресурсы и технологии

### Сейсмокардиография (SCG)
*   **[Что такое SCG?](https://heartscan.app/about-scg)** — подробное объяснение технологии захвата механических вибраций сердца с помощью акселерометра. В отличие от ЭКГ (электрические сигналы), SCG измеряет механическую работу сердца (открытие/закрытие клапанов, сокращение желудочков).
*   **[Руководство по интерпретации](https://heartscan.app/primary-care-guide)** — практический визуальный гид по формам волн SCG для врачей первичного звена. Помогает распознавать паттерны (PAC, PVC, мерцательная аритмия) на основе механических сигналов.

### Open Source
*   **[OpenSCG](https://github.com/HeartScan/openSCG)** — открытая инфраструктура для превращения любого смартфона в датчик сейсмокардиографии. Проект нацелен на устранение барьера стоимости оборудования в телемедицине.

---
*Отказ от ответственности: Данное приложение предназначено для образовательных и демонстрационных целей и не заменяет визит к врачу.*
